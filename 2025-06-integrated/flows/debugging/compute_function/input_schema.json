{
  "type": "object",
  "required": [
    "compute",
    "input_gcs"
  ],
  "properties": {
    "compute": {
      "type": "object",
      "title": "Compute options",
      "required": [
        "endpoint_id",
        "function_id"
      ],
      "properties": {
        "endpoint_id": {
          "type": "string",
          "title": "GCE compute endpoint ID",
          "description": "A compute endpoint that is able to run the validation function"
        },
        "function_id": {
          "type": "string",
          "title": "GCE compute function ID",
          "description": "A GCE registered compute function"
        }
      }
    },
    "input_gcs": {
      "type": "object",
      "title": "Specify where to find a data file fetched by this function",
      "format": "globus-collection",
      "required": [
        "id",
        "path"
      ],
      "properties": {
        "id": {
          "type": "string",
          "title": "Source Collection ID",
          "format": "uuid",
          "description": "The UUID for the collection which serves as the source of the data for the two-stage Transfer"
        },
        "path": {
          "type": "string",
          "title": "Source Collection Path",
          "description": "The path on the source collection for the data"
        }
      }
    }
  },
  "additionalProperties": false
}