{
  "type": "object",
  "required": [
    "source",
    "destination",
    "compute",
    "search_index_id",
    "search_metadata"
  ],
  "properties": {
    "source": {
      "type": "object",
      "title": "Source",
      "format": "globus-collection",
      "required": [
        "id",
        "path"
      ],
      "properties": {
        "id": {
          "type": "string",
          "title": "Source Collection ID",
          "format": "uuid",
          "description": "The UUID for the collection which serves as the source of the data for the two-stage Transfer"
        },
        "path": {
          "type": "string",
          "title": "Source Collection Path",
          "description": "The path on the source collection for the data"
        }
      },
      "description": "The data's origin",
      "additionalProperties": false
    },

    "destination": {
      "type": "object",
      "title": "Destination",
      "format": "globus-collection",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "title": "Destination Collection ID",
          "format": "uuid",
          "description": "The UUID for the destination collection"
        },
        "path": {
          "type": "string",
          "title": "Source Collection Path",
          "description": "IGNORED: This field is only present due to a bug in the globus flows UI Jun2025"
        }
      },
      "description": "Where to send the data",
      "additionalProperties": false
    },

    "compute": {
      "type": "object",
      "title": "Compute options",
      "description": "TESTING PURPOSES ONLY (will later be moved into flow as private params)",
      "required": ["endpoint_id", "function_id"],
      "properties": {
        "endpoint_id": {
          "type": "string",
          "title": "GCE compute endpoint ID",
          "description": "A bespoke compute endpoint deployed to run validation function (must have credentials to read dest collection via HTTPS)"
        },
        "function_id": {
          "type": "string",
          "title": "GCE validation function ID",
          "description": "A GCE compute function that performs file validation- TODO move to private params / hardcoded flow value"
        }
      }
    },

    "search_index_id": {
      "type": "string",
      "title": "Search index ID",
      "description": "TESTING PURPOSES ONLY (will later be moved into flow as private params)",
      "default": "8cd76948-c803-483b-88aa-cdd2f6a4cbe6"
    },

    "search_metadata": {
      "type": "object",
      "title": "Search metadata",
      "description": "Metadata that will be used to populate a search index. Simplified from datacite 4.6; in future we will add custom extra fields.",
      "properties": {
        "creators": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string"
              },
              "givenName": {
                "title": "Given Name",
                "type": "string"
              },
              "familyName": {
                "title": "Family name",
                "type": "string"
              },
              "affiliation": {
                "type": "object",
                "properties": {
                  "name": {
                    "title": "Affiliation",
                    "description": "Institution / employer for this author",
                    "type": "string"
                  }
                },
                "required": [
                  "name"
                ]
              }
            },
            "required": [
              "name"
            ]
          }
        },
        "title": {
          "type": "string",
          "title": "Title",
          "description": "A short human-friendly title for this dataset"
        },
        "description": {
          "type": "string",
          "title": "Description",
          "description": "A one-paragraph description shown in search results"
        },
        "publisher_name": {
          "type": "string",
          "title": "Produced by",
          "description": "The name of the institution that produced the resource"
        }
      },
      "required": [
        "identifier",
        "creators",
        "title",
        "description",
        "publisher_name"
      ],
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}